using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourcePanel : MonoBehaviour
{
    [SerializeField]
    private RectTransform tierOnePanel;
    [SerializeField]
    private RectTransform tierTwoPanel;
    [SerializeField]
    private RectTransform tierThreePanel;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
