using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CaveLord : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
        Health = 30;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
